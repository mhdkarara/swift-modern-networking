// Example of how to use the APIClient in a ViewModel
/*
 let client = APIClient()
 let request = APIRequest(path: "/posts")
 
 do {
    let posts = try await client.fetch(request, as: [Post].self)
    print("Fetched \(posts.count) posts")
 } catch {
    print("Error: \(error)")
 }
*/
